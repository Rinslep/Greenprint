# requirements.txt
#
# TODO: Pin all versions once confirmed working together.
#
# Core pipeline
# - pydantic>=2.0           — schema validation models
# - pydantic-settings        — settings/config management
# - python-dotenv            — .env support in dev
# - networkx                 — crafting graph and lane model (DiGraph, find_cycle)
# - sqlalchemy>=2.0          — ORM for blueprints, motifs, review_queue tables
# - alembic                  — database migrations
# - psycopg2-binary          — PostgreSQL driver (production); sqlite3 is stdlib for dev
#
# API
# - fastapi                  — REST API framework
# - uvicorn[standard]        — ASGI server
# - slowapi                  — per-IP rate limiting middleware for FastAPI
#
# Scrapers
# - requests                 — HTTP for factorio_prints and factorio_school
# - beautifulsoup4           — HTML parsing for forums scraper
# - praw                     — Reddit API client
# - playwright               — JS-rendered pages on forums (install chromium separately)
# - robotparser              — stdlib, but confirm it's accessible as urllib.robotparser
#
# Testing
# - pytest
# - pytest-cov
# - httpx                    — async test client for FastAPI
